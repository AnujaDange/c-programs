#include<stdio.h>
int main()
{
    int x;
    char a;
    float p,q;
    printf("\n a is stored at address %u",&a);
    printf("\n x is stored at address %u",&x);
    printf("\n p is stored at address %u",&p);
    printf("\n q is stored at address %u",&q);
    return(0);
}
